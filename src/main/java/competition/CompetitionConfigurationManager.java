package competition;

import dominion.core.initialisation.GameConfiguration;
import dominion.core.initialisation.GameConfigurationManager;

public class CompetitionConfigurationManager extends GameConfigurationManager {
    public CompetitionConfigurationManager(GameConfiguration configuration) {
        super(configuration);
    }

    @Override
    public void initialiseGame() {
        // Empty function
    }
}
